pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        jcenter()

        maven { url 'https://jitpack.io' }
    }
}

includeBuild("build-logic")

include ':kronos'

include 'extension:property-api'
include 'extension:json'
include 'extension:json-kotlinx'

include 'source:firebase'
